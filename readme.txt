Das Casten befindet sich im Task-Model

Änderungen wirken sich auf die View-Dateien aus (index,show (jeweils tasks) und form-tasks (in components)

Bsp:
$task(s)->format("d.m.Y")

------------------------------

Models mit hasMany und belongsTo